import React from "react";
const API_URL = process.env.REACT_APP_API_URL;

const Login = () => {
  let loading = false;
  const handleChaange = () => { };
  const handleSubmit = () => { };
  return (
    <div>LOGIN</div>
  );
};

export default Login;
